# Stock Earnings Monitor

## Data contract
This project is a data system for US stock earnings monitoring.
The agent's job is to maintain and advance the data, not the code framework.

- `data/earnings_history.jsonl` — append-only actual EPS/revenue per ticker per period; also the growth baseline
- `data/alerts.jsonl` — emitted alert records
- `data/cik_tickers.json` — cached SEC ticker->CIK map (regenerable, gitignored)

## Authoritative data sources

### Report calendar + actual EPS
- NASDAQ public API: `https://api.nasdaq.com/api/calendar/earnings?date=YYYY-MM-DD`
- Confirmed real schema (2026-07): `symbol`, `fiscalQuarterEnding` (e.g. "Jun/2026"),
  `epsForecast`, and once reported: `eps`, `surprise`. **No revenue field of any
  kind is present on this endpoint.** The row also carries no date field —
  the date is only the query parameter.

### Actual revenue
- SEC EDGAR `companyfacts` API: `https://data.sec.gov/api/xbrl/companyfacts/CIK{cik}.json`
- CIK looked up from SEC's official `https://www.sec.gov/files/company_tickers.json`
  (cached to `data/cik_tickers.json`)
- Revenue tag: `RevenueFromContractWithCustomerExcludingAssessedTax`, falling
  back to the legacy `Revenues` tag. Only entries spanning ~1 fiscal quarter
  (80-100 days) are used, to exclude YTD/annual cumulative figures.
- Can lag the earnings release by a day or two, and is unavailable for
  filers that don't submit 10-Q/10-K (e.g. 20-F foreign private issuers).

## Data pipeline

`src/monitor.py` is the single entrypoint, meant to run every minute via cron:

1. Fetch today's NASDAQ calendar (US/Eastern date).
2. For each row with an actual EPS that isn't already recorded in
   `data/earnings_history.jsonl` for that ticker+period:
   a. Look up the ticker's latest prior recorded actual as the baseline.
   b. Fetch matching quarterly revenue from EDGAR.
   c. Score EPS growth % and revenue growth % vs. the baseline
      (`src/scorer.py`).
   d. Append the new record to `data/earnings_history.jsonl` regardless of
      outcome (it becomes tomorrow's baseline).
   e. If both growth checks pass, append to `data/alerts.jsonl` and send a
      Telegram message immediately.

A ticker's first-ever recorded report has no baseline and never alerts.

## Invariant
All work must be executable with plain `python src/<name>.py`.
No new dependency on FastAPI or other apps unless requested.

## Conventions
- Use `requirements.txt` for dependencies only.
- Use `data/` for JSONL outputs.
- Do not hardcode credentials. Read from env or `config.yaml`.
- Telegram sending is the Hermes gateway usage here (`hermes send telegram <chat_id> <message>`).
- Keep changes minimal and focused.

## Suggested run
```
* * * * * cd ~/stock-earnings-monitor && python3 src/monitor.py >> logs/cron.log 2>&1
```
