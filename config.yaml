fetcher:
  lookback_days: 1
  save_path: data/earnings_history.jsonl

scorer:
  save_path: data/alerts.jsonl

telegram:
  platform: telegram
  chat_target: "<set_chat_id_or_label>"
  min_confidence: medium
