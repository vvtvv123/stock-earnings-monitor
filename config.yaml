history:
  path: data/earnings_history.jsonl

alerts:
  path: data/alerts.jsonl

# EPS and revenue actuals come from Finnhub's earnings calendar API.
# Set the FINNHUB_API_KEY environment variable -- do not put the key here.

scorer:
  min_eps_growth_pct: 5.0
  min_revenue_growth_pct: 1.0

telegram:
  chat_id: "8782198462"
